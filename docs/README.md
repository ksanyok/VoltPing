# VoltPing - Universal Power Monitoring System

<p align="center">
  <img src="https://img.shields.io/badge/PHP-8.0+-blue.svg" alt="PHP 8.0+">
  <img src="https://img.shields.io/badge/Python-3.8+-green.svg" alt="Python 3.8+">
  <img src="https://img.shields.io/badge/License-MIT-yellow.svg" alt="License">
</p>

**VoltPing** — універсальна система моніторингу електропостачання на базі розумних розеток Tuya. Отримуйте миттєві сповіщення про відключення світла та аномальну напругу прямо в Telegram!

## ✨ Можливості

- ⚡ **Моніторинг в реальному часі** — відстежує напругу та стан живлення
- 📱 **Telegram бот** — зручні сповіщення та інтерактивне керування
- 🏠 **Локальне підключення** — без лімітів API (30k/місяць)
- ☁️ **Cloud API** — надійний резервний метод
- 📊 **Статистика** — історія подій, uptime, графіки
- 📅 **Графіки відключень** — парсинг розкладів з каналів
- 🔐 **Адмін-панель** — веб-інтерфейс для управління
- 🔑 **Auto Local Key** — автоматичне визначення ключа пристрою

## 🚀 Швидкий старт

### Варіант 1: Веб-інсталлер (рекомендовано) 🌐

Найпростіший спосіб — через веб-інтерфейс:

1. Завантажте `install.php` на ваш сервер:
```bash
wget https://raw.githubusercontent.com/ksanyok/VoltPing/main/install.php
```

2. Відкрийте в браузері:
```
https://your-domain.com/install.php
```

3. Пройдіть 6 кроків візуального майстра налаштування

4. **Видаліть `install.php` після завершення!**

---

### Варіант 2: Bash-інсталлер (для CLI) 💻

Для тих, хто любить командний рядок:

```bash
curl -sSL https://raw.githubusercontent.com/ksanyok/VoltPing/main/install.sh | bash
```

Або завантажте і запустіть вручну:

```bash
wget https://raw.githubusercontent.com/ksanyok/VoltPing/main/install.sh
chmod +x install.sh
./install.sh /path/to/install
```

---

### Варіант 3: Ручна установка

1. Клонуйте репозиторій:
```bash
git clone https://github.com/ksanyok/VoltPing.git
cd VoltPing
```

2. Скопіюйте та налаштуйте конфігурацію:
```bash
cp env.example .env
nano .env
```

3. Налаштуйте веб-сервер або запустіть вбудований:
```bash
php -S localhost:8080
```

4. Встановіть webhook для бота:
```bash
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-domain.com/src/bot.php"
```

5. Додайте cron завдання:
```bash
crontab -e
# Додайте рядок:
* * * * * php /path/to/VoltPing/src/watch_power.php >> /dev/null 2>&1
```

## ⚙️ Конфігурація

### Telegram Bot

1. Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
2. Надішліть `/newbot`
3. Введіть ім'я бота (наприклад: `Світло Мій Будинок`)
4. Введіть username (наприклад: `svitlo_myhouse_bot`)
5. Скопіюйте токен

**Отримання Chat ID:**
1. Створіть канал або групу
2. Додайте бота як адміністратора
3. Надішліть повідомлення
4. Відкрийте: `https://api.telegram.org/bot<TOKEN>/getUpdates`
5. Знайдіть `"chat":{"id": -100XXXXXXXXXX}`

### Tuya API

1. Зареєструйтесь на [iot.tuya.com](https://iot.tuya.com/)
2. Створіть Cloud Project
3. Виберіть регіон (EU для України)
4. Підключіть Smart Life App:
   - Devices → Link Tuya App Account
5. Скопіюйте Access ID та Access Secret з Overview
6. Знайдіть Device ID вашої розетки

### Локальне підключення (без лімітів)

**Переваги:**
- ❌ Без ліміту 30,000 запитів/місяць
- ⚡ Швидша відповідь (1-2 сек замість 5-10)
- 🔒 Працює при відсутності інтернету (з VPN/тунелем)

**Налаштування:**

1. **Local Key** — визначається автоматично при першому запуску, або:
   - Tuya IoT → Devices → All Devices → View Details → Local Key
   - Або: `pip install tinytuya && python -m tinytuya wizard`

2. **IP адреса пристрою:**
   - Локальна: знайдіть в налаштуваннях роутера або Smart Life App
   - Публічна: налаштуйте port forwarding (див. нижче)

3. **Port Forwarding:**
   ```
   Зовнішній порт: 6668 → Внутрішній: 192.168.x.x:6668 (TCP)
   ```

## 📱 Формат повідомлень для парсингу графіків

Якщо ви хочете парсити графіки відключень з Telegram каналу, повідомлення мають бути в одному з форматів:

### Формат 1: Стандартний

```
📅 Графік відключень на 05.02.2026

🔴 Черга 1: 00:00-06:00, 12:00-18:00
🟡 Черга 2: 06:00-12:00, 18:00-24:00
🟢 Черга 3: без відключень

⚡ Ваша черга: 1
```

### Формат 2: Скорочений

```
Графік на 05.02:
1 черга: 8-12, 20-24
2 черга: 4-8, 16-20
```

### Формат 3: Текстовий

```
Сьогодні відключення:
- з 08:00 до 12:00
- з 18:00 до 22:00
```

**Підтримувані формати часу:**
- `HH:MM-HH:MM` — наприклад: `08:00-12:00`
- `HH-HH` — наприклад: `8-12`
- `з HH:MM до HH:MM` — наприклад: `з 08:00 до 12:00`

## 📂 Структура проекту

```
voltping/
├── install.sh          # Інсталятор
├── src/
│   ├── config.php      # Конфігурація
│   ├── watch_power.php # Основний скрипт моніторингу
│   ├── bot.php         # Telegram webhook handler
│   ├── admin.php       # Адмін-панель
│   ├── tuya_client.php # Tuya Cloud API клієнт
│   ├── tuya_local_client.php # Tuya Local клієнт
│   ├── tuya_local_poll.py    # Python скрипт для tinytuya
│   ├── schedule_parser.php   # Парсер графіків
│   └── .env            # Конфігурація (створюється інсталятором)
├── docs/
│   ├── README.md       # Ця документація
│   ├── SETUP.md        # Детальна інструкція
│   ├── TUYA_SETUP.md   # Налаштування Tuya
│   └── PORT_FORWARD.md # Port Forwarding
└── logs/               # Логи
```

## 🔧 Змінні середовища (.env)

```env
# Project
PROJECT_NAME="Мій Будинок"
CHANNEL_BASE_TITLE="Мій Будинок — Світло ⚡ Напруга"

# Admin
ADMIN_PASSWORD="your-secure-password"

# Telegram
TG_BOT_TOKEN="123456789:ABCdefGHI..."
TG_CHAT_ID="-1001234567890"
TG_ADMIN_ID="123456789"

# Tuya Cloud API
TUYA_ENDPOINT="https://openapi.tuyaeu.com"
TUYA_ACCESS_ID="your-access-id"
TUYA_ACCESS_SECRET="your-secret"
TUYA_DEVICE_ID="bf1234567890"

# Tuya Local (optional)
TUYA_MODE="cloud"          # cloud | local | hybrid
TUYA_LOCAL_KEY=""          # Auto-detected if empty
TUYA_LOCAL_IP="192.168.1.100"
TUYA_PUBLIC_IP=""          # For external access
TUYA_LOCAL_VERSION="3.5"

# Monitoring
CHECK_INTERVAL_SECONDS="60"
NOTIFY_REPEAT_MINUTES="60"
V_WARN_LOW="207"
V_WARN_HIGH="253"
V_CRIT_LOW="190"
V_CRIT_HIGH="260"

# Schedule Parsing
SCHEDULE_CHANNEL_ID=""
SCHEDULE_QUEUE="1"
```

## 📊 API Endpoints

| Endpoint | Опис |
|----------|------|
| `GET /watch_power.php` | Перевірка стану (можна викликати з cron) |
| `GET /watch_power.php?force=1` | Примусова перевірка |
| `POST /bot.php` | Telegram webhook |
| `GET /admin.php` | Адмін-панель |
| `GET /admin.php?api=status` | JSON статус |
| `GET /admin.php?api=events` | JSON історія подій |

## 🤖 Команди бота

| Команда | Опис |
|---------|------|
| `/start` | Почати роботу, підписатися на сповіщення |
| `/stop` | Відписатися від сповіщень |
| `/status` | Поточний статус |
| `/stats` | Статистика за сьогодні |
| `/help` | Довідка |

## ❓ FAQ

**Q: Скільки коштує система?**
A: Система безкоштовна. Потрібна лише розумна розетка Tuya (~$10-15).

**Q: Які розетки підтримуються?**
A: Будь-які з підтримкою Tuya/Smart Life з вимірюванням напруги.

**Q: Чому не працює локальне підключення?**
A: Перевірте: 1) Local Key правильний, 2) IP доступна, 3) Порт 6668 відкритий.

**Q: Як часто опитується пристрій?**
A: За замовчуванням кожну хвилину. Можна налаштувати в .env.

## 📄 Ліцензія

MIT License. Дивіться [LICENSE](LICENSE) для деталей.

## 🙏 Подяки

- [tinytuya](https://github.com/jasonacox/tinytuya) — Python бібліотека для Tuya
- [SvitloLevada](https://github.com/ksanyok/SvitloLevada) — натхнення для проекту

---

<p align="center">
  Made with ⚡ in Ukraine
</p>
